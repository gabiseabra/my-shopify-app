{
  "name": "my-shopify-app",
  "version": "1.0.0",
  "private": true,
  "author": "hey@gabsieabra.dev",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build -ws"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
